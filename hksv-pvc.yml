apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hcsv-config-pvc
  namespace: hksv-scrypted
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: "hcsv-config"